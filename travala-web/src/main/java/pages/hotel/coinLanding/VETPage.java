package pages.hotel.coinLanding;

import commons.AbstractPage;
import org.openqa.selenium.WebDriver;

public class VETPage extends AbstractPage {
  WebDriver driver;

  public VETPage(WebDriver driver_) {
    this.driver = driver_;
  }
}
